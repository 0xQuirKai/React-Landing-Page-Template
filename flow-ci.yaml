name: React CI Pipeline
steps:
  - name: Install Dependencies
    commands:
      - npm install  # Or yarn install

  - name: Run Tests
    commands:
      - npm test -- --coverage --ci # Or yarn test --coverage
